import os
import pygame

# ------------------------------------------------------------------ #
#  Пути к ресурсам                                                     #
# ------------------------------------------------------------------ #
BASE_DIR   = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
ASSETS_DIR = os.path.join(BASE_DIR, "assets")
IMAGES_DIR = os.path.join(ASSETS_DIR, "images")
FONTS_DIR  = os.path.join(ASSETS_DIR, "fonts")
SOUNDS_DIR = os.path.join(ASSETS_DIR, "sounds")

# ------------------------------------------------------------------ #
#  Окно                                                                #
# ------------------------------------------------------------------ #
TITLE  = "Platformer"
WIDTH  = 1280
HEIGHT = 720
FPS    = 60

# ------------------------------------------------------------------ #
#  Цвета                                                               #
# ------------------------------------------------------------------ #
WHITE     = (255, 255, 255)
BLACK     = (0,   0,   0  )
GRAY      = (160, 160, 160)
DARK_GRAY = (50,  50,  50 )
RED       = (220, 60,  60 )
GREEN     = (60,  200, 80 )
BLUE      = (60,  100, 220)
YELLOW    = (255, 220, 0  )
SKY_BLUE  = (100, 180, 240)
DARK_BLUE = (15,  30,  70 )

# ------------------------------------------------------------------ #
#  Уровень                                                             #
# ------------------------------------------------------------------ #
LEVEL_WIDTH  = 3840   # 3 экрана по горизонтали
LEVEL_HEIGHT = HEIGHT  # без вертикального скролла
LIVES        = 3       # начальное количество жизней

# ------------------------------------------------------------------ #
#  Физика                                                              #
# ------------------------------------------------------------------ #
GRAVITY        = 1200   # px / s²
PLAYER_SPEED   = 300    # px / s
JUMP_FORCE     = -650   # px / s  (отрицательный — вверх)
MAX_FALL_SPEED = 900    # px / s

# ------------------------------------------------------------------ #
#  Спрайт игрока                                                       #
# ------------------------------------------------------------------ #
PLAYER_FRAME_W = 48
PLAYER_FRAME_H = 48

# Длительность одного кадра (секунды) для каждой анимации
ANIM_SPEED = {
    "idle": 0.15,
    "run":  0.07,
    "jump": 0.12,
    "fall": 0.12,
}

# ------------------------------------------------------------------ #
#  Настройки по умолчанию                                              #
# ------------------------------------------------------------------ #
DEFAULT_SETTINGS = {
    "music_volume": 0.5,
    "sfx_volume":   0.7,
    "resolution":   (WIDTH, HEIGHT),
    "controls": {
        "left":  pygame.KSCAN_A,
        "right": pygame.KSCAN_D,
        "jump":  pygame.KSCAN_SPACE,
    },
}
