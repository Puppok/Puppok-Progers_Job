// Условия
// if(условие) {
//      действие, если условие true
// }
// else {
//      действие, если условие true
// }

let num1 = 12
let num2 = 20

if(num1 < num2) {
    console.log('Четко')
}
else {
    console.log('Не четко')
}

// else if, множественное условие
if(num1 > num2) {
    console.log('Четко')
}
else if(num1 < num2) {
    console.log('Не очень четко')
}
else {
    console.log('Очко')
}

// Оператор выбора (switch)
let choice = 'kagjhsdga'

switch(choice) {
    case 'чпок':
        console.log('получился чпок')
        break
    case 'чо каво':
        console.log('свари супчик')
        break
    default:
        console.log('пора спать')
        break
}

// Тернарный оператор
// переменная = условие ? если true : если false
let result = 10 > 12 ? 'да' : 'нет'
console.log(result)


// Логические операторы
// && - И
// || - ИЛИ
// ! - НЕ

// 1.
// Дана переменная year с годом. Определите, является ли год високосным.
//
// Правила:
// Год високосный, если делится на 4
// НО если делится на 100 — он НЕ високосный
// НО если делится на 400 — он ВСЁ РАВНО високосный
// Присвойте переменной isLeapYear значение true или false

// 2.
// Камень-ножницы-бумага
// Даны переменные player1 и player2 с выборами игроков: "rock", "paper" или "scissors".
//
// Определите результат игры и присвойте переменной winner значение:
// "player1" - если выиграл первый игрок
// "player2" - если выиграл второй игрок
// "draw" - если ничья
// Правила: камень бьет ножницы, ножницы бьют бумагу, бумага бьет камень
//
// Используйте switch для решения

// 3.
// Определение типа треугольника
// Даны три переменные a, b, c — длины сторон треугольника.
//
// Определите тип треугольника и присвойте переменной triangleType значение:
//
// "Не треугольник" - если треугольник не существует (сумма любых двух сторон должна быть больше третьей)
// "Равносторонний" - все три стороны равны
// "Равнобедренный" - две стороны равны
// "Разносторонний" - все стороны разные
// Важно: Сначала проверьте, существует ли треугольник!

// 4.
// Проверка корректности даты
// Даны переменные day, month, year.
//
// Определите, является ли дата корректной, и присвойте переменной isValidDate значение true или false.
//
// Правила:
// Месяц должен быть от 1 до 12
// День должен быть от 1 до максимального для этого месяца
// В феврале 28 дней (29 в високосном году)
// В месяцах 4, 6, 9, 11 — по 30 дней
// В остальных месяцах — по 31 дню
// Год високосный, если делится на 4, но не на 100, или делится на 400

// 5.
// Система скидок интернет-магазина
// Даны переменные:
//
// totalPrice — общая сумма покупки
// isPremiumMember — премиум участник (true/false)
// itemsCount — количество товаров в корзине
// couponType — тип купона: "SUMMER", "WINTER", "VIP" или null
// Рассчитайте итоговую скидку в процентах и присвойте её переменной discount. Правила:
//
// Базовая скидка по сумме:
// Сумма < 1000: 0%
// 1000 ≤ сумма < 5000: 5%
// 5000 ≤ сумма < 10000: 10%
// Сумма ≥ 10000: 15%
// Премиум участники: +5% к базовой скидке
// Количество товаров: если больше 10 товаров: +3%
// Купоны:
// "SUMMER": +10% (только если сумма < 5000)
// "WINTER": +7%
// "VIP": +15% (только для премиум участников)
// Максимальная скидка: не более 50%