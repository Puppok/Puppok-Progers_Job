#include <iostream> // подключение библиотеки in out stream (ввод / вывод)
using namespace std; // использование набора стандартных команд std

int main() { // главная функция программы
    // === 1. Переменные ===
    // тип_данных имя = значение;
    int number = 437;

    // --- Типы данных ---
    // 1. int - целые числа
    int num = 12;

    // 2. float - дроби
    float shell = 5.89;

    // 3. double - дроби (больше диапазон значений)
    double shell_2 = 12515.457864957;

    // 4. char - один символ (одиночные кавычки)
    char symb = 'G';

    // 5. string - строки (двойные кавычки)
    string str = "Привет, чивапчичи 288!";

    // 6. bool - логический тип (true / false)
    bool is_weekend = false;

    // Принципы именования переменных
    // getdatafromtable - очень плохо
    // 1. Camel Case -> getDataFromTable
    // 2. Underscore Case -> get_data_from_table

    return 0; // успешное завершение программы
}
